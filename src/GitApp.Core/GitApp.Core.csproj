<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>


  <ItemGroup>
    <!-- ParseStatus is internal because nothing outside the service should
         call it, but it is the single most bug-prone function in the app and
         must be directly testable. -->
    <InternalsVisibleTo Include="GitApp.Core.Tests" />
  </ItemGroup>

</Project>
